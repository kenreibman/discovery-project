---
phase: 03-pdf-parsing-request-extraction
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified: []
autonomous: false
requirements: [UPLD-03, UPLD-04, UPLD-05]

must_haves:
  truths:
    - "Jessica can upload a discovery PDF and see individually extracted requests listed on the case detail page"
    - "The system identifies whether the document is an RFP or interrogatory set"
    - "Jessica can re-extract if the initial extraction looks wrong"
    - "The auto-naming feature populates the case name from the PDF caption"
    - "The Generate Responses button is visible but disabled"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 3 flow end-to-end: upload a discovery PDF, observe automatic extraction with loading state, verify request list display with expand/collapse, confirm discovery type detection, test re-extract, and confirm auto-naming.

Purpose: Phase 3's success criteria require Jessica to see extracted requests and verify them. This checkpoint confirms the full pipeline works from upload through display before marking the phase complete.

Output: User verification that all Phase 3 success criteria are met.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pdf-parsing-request-extraction/03-CONTEXT.md
@.planning/phases/03-pdf-parsing-request-extraction/03-UI-SPEC.md
@.planning/phases/03-pdf-parsing-request-extraction/03-01-SUMMARY.md
@.planning/phases/03-pdf-parsing-request-extraction/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of Phase 3 extraction pipeline</name>
  <files></files>
  <action>
    Present the following verification checklist to the user. This is a human-verify checkpoint -- no code changes. The user tests the running application.

    What was built across Plans 01 and 02:
    - Database schema with extracted_requests table and document sub-type column
    - Claude-powered extraction server action (base64 PDF + structured output)
    - Automatic extraction trigger after upload and classification
    - Extracted requests list UI with expand/collapse, loading state, error handling
    - Discovery sub-type badge with override dropdown
    - Re-extract functionality
    - Auto-naming from PDF caption
    - Disabled "Generate Responses" button (Phase 4 placeholder)

    Verification steps for the user:

    Start the dev server if not running: `npm run dev`

    **Test 1: Upload and Automatic Extraction (UPLD-03, D-02)**
    1. Navigate to the dashboard at http://localhost:3000/dashboard
    2. Upload the Swan RFP PDF (`samples/Swan - First RFP.pdf`) via drag-and-drop or file picker
    3. Wait for upload -> classification -> extraction (should see "Extracting..." status briefly)
    4. After case creation and navigation to the case detail page, verify:
       - Extracted Requests section appears below the document list
       - Requests are listed individually with numbers (expect ~33 requests)
       - Each request shows a truncated preview (~100 chars) with "..." if longer
       - Clicking a long request row expands to show full text
       - Clicking again collapses back to preview

    **Test 2: Discovery Type Detection (UPLD-05, D-13, D-14)**
    5. Verify a badge shows "RFP" next to the "Extracted Requests" heading
    6. Click the badge to open the sub-type dropdown
    7. Switch to "Interrogatory" -- badge should update immediately
    8. Switch back to "RFP"

    **Test 3: Auto-Naming (D-09, D-10)**
    9. Check if the case name was automatically set from the PDF (should show something like "Swan v. Dollar Tree" if the case was created without a name)

    **Test 4: Re-Extract (D-04)**
    10. Click the "Re-extract" button
    11. Verify loading state appears (skeleton rows + "Extracting requests..." text)
    12. Wait for re-extraction to complete
    13. Verify request list reappears with the same requests

    **Test 5: Generate Responses Button (D-05)**
    14. Verify "Generate Responses" button is visible at the bottom of the request list
    15. Verify it is disabled (50% opacity, cursor shows not-allowed)

    **Test 6: Scanned PDF Handling (UPLD-04, D-06)**
    16. If a scanned PDF is available for testing, upload it
    17. Verify extraction attempts without showing a warning upfront
    18. If extraction fails, verify the error message: "Could not extract requests from this PDF. The document may be a poor-quality scan."
    19. Verify "Retry Extraction" and "Upload new" buttons appear

    **Test 7: Request Count**
    20. Verify the request count in parentheses matches the number of listed requests (e.g., "(33)")

    Resume signal: Type "approved" if all tests pass, or describe which tests failed and what you observed.
  </action>
  <verify>User confirms all 7 test groups pass by typing "approved"</verify>
  <done>All Phase 3 success criteria verified by user: individual request extraction, scanned PDF handling, discovery type detection, and extraction verification UI all functional.</done>
</task>

</tasks>

<verification>
All Phase 3 success criteria from ROADMAP.md:
1. After uploading a digital PDF, Jessica sees each individual numbered request listed separately
2. After uploading a scanned/OCR PDF, the system still extracts requests without failing silently
3. The system correctly identifies whether the uploaded document is an RFP or an interrogatory set
4. Jessica can inspect the extracted request list and catch parsing errors before generation starts
</verification>

<success_criteria>
User confirms all verification steps pass. Phase 3 requirements (UPLD-03, UPLD-04, UPLD-05) are met as observable behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pdf-parsing-request-extraction/03-03-SUMMARY.md`
</output>
