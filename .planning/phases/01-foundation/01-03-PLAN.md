---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(protected)/layout.tsx
  - src/app/(protected)/dashboard/page.tsx
  - src/components/dashboard-greeting.tsx
  - src/components/upload-zone-shell.tsx
  - src/components/sidebar-nav.tsx
autonomous: false
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Authenticated user sees dashboard with time-of-day greeting"
    - "Sidebar shows 'No cases yet' empty state"
    - "Sidebar has subtle logout button at bottom that ends session"
    - "Drop zone shell renders with dashed border and drag-hover visual feedback"
    - "Session persists across browser refresh (user stays logged in)"
  artifacts:
    - path: "src/app/(protected)/layout.tsx"
      provides: "Dashboard layout with sidebar"
      contains: "SidebarNav"
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "Dashboard page with greeting and drop zone"
      contains: "DashboardGreeting"
    - path: "src/components/dashboard-greeting.tsx"
      provides: "Time-of-day greeting component"
      contains: "Good morning"
    - path: "src/components/upload-zone-shell.tsx"
      provides: "Non-functional drop zone UI placeholder"
      contains: "Drop files here"
    - path: "src/components/sidebar-nav.tsx"
      provides: "Sidebar with case list and logout"
      contains: "No cases yet"
  key_links:
    - from: "src/app/(protected)/layout.tsx"
      to: "src/components/sidebar-nav.tsx"
      via: "component import and render"
      pattern: "SidebarNav"
    - from: "src/components/sidebar-nav.tsx"
      to: "src/lib/auth.ts"
      via: "signOut import for logout"
      pattern: "signOut"
    - from: "src/app/(protected)/dashboard/page.tsx"
      to: "src/components/dashboard-greeting.tsx"
      via: "component import"
      pattern: "DashboardGreeting"
---

<objective>
Build the Claude-inspired dashboard shell with sidebar navigation, time-of-day greeting, upload drop zone placeholder, case list empty state, and logout functionality. This is the authenticated experience Jessica sees after login.

Purpose: Delivers the post-login experience (D-04 through D-08, D-13). Validates AUTH-02 (protected routes work), AUTH-03 (session persists), and provides the visual checkpoint for the entire Phase 1 UI.

Output: Dashboard at / with sidebar (case list + logout) and main area (greeting + drop zone shell).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- From Plan 01 — auth and db exports -->
From src/lib/auth.ts:
```typescript
export const { handlers, auth, signIn, signOut } = NextAuth({...})
```

From src/lib/db/schema.ts:
```typescript
export const cases = sqliteTable("cases", {
  id: text("id").primaryKey(),
  userId: text("user_id").notNull().references(() => users.id),
  name: text("name"), // nullable
  createdAt: integer("created_at", { mode: "timestamp" }),
  updatedAt: integer("updated_at", { mode: "timestamp" }),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard layout with sidebar, greeting, drop zone shell, and logout</name>
  <files>
    src/app/(protected)/layout.tsx, src/app/(protected)/dashboard/page.tsx,
    src/components/dashboard-greeting.tsx, src/components/upload-zone-shell.tsx,
    src/components/sidebar-nav.tsx, src/app/page.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Dashboard contract — full layout spec, sidebar spec, drop zone states, interaction contracts)
    .planning/phases/01-foundation/01-CONTEXT.md (D-04 layout, D-05 drop zone, D-06 greeting, D-07 case entries, D-08 empty state, D-13 logout)
    .planning/phases/01-foundation/01-RESEARCH.md (time-of-day greeting code example)
    src/lib/auth.ts (verify auth, signOut exports exist)
    src/lib/db/schema.ts (cases table shape for future case list queries)
    src/app/globals.css (verify sidebar CSS variables exist from Plan 01)
    src/components/ui/button.tsx (shadcn button API)
    src/components/ui/separator.tsx (shadcn separator API)
  </read_first>
  <action>
    1. Update `src/app/page.tsx` — Root route redirects to /dashboard:
       ```typescript
       import { redirect } from "next/navigation"
       export default function Home() {
         redirect("/dashboard")
       }
       ```

    2. Create `src/app/(protected)/layout.tsx` — Dashboard layout per D-04 (Claude-inspired layout):
       - Server component that calls `auth()` from `@/lib/auth` to get the session
       - If no session, redirect to "/login" (belt-and-suspenders with middleware)
       - Layout: flex row, full viewport height
       - Left: `<SidebarNav />` component, 280px wide, fixed
       - Right: main content area, flex-1, overflow-y-auto
       - Main content: centered container with max-w-[640px] mx-auto
       - Pass session user info to SidebarNav if needed

    3. Create `src/components/sidebar-nav.tsx` — Per UI-SPEC Dashboard Sidebar contract:
       - "use client" (needs signOut action and future interactivity)
       - Structure (top to bottom per UI-SPEC):
         a. App name "Discovery Drafter" — 16px, font-semibold (weight 600), px-6 py-6 (24px)
         b. Separator (shadcn `<Separator />`)
         c. Case list area — flex-1, overflow-y-auto, scrollable
            - For Phase 1: hardcoded empty state only
            - Empty state: "No cases yet" centered, text-sm (14px), text-muted-foreground (#78736A) — per D-08
            - Future: map over cases array, each entry shows name (16px, truncated) + date below (14px, text-muted-foreground), px-3 py-2, left border 3px transparent (active = accent border via border-l-[3px])
         d. Bottom section — sticky to bottom:
            - Separator
            - Logout button: flex row, items-center, gap-2, px-6 py-4
              - lucide `LogOut` icon (size 16), text-muted-foreground
              - "Log out" text, text-sm (14px), text-muted-foreground
              - Ghost button style (no background, hover shows subtle bg)
              - onClick: call server action to sign out and redirect to /login. Use `signOut` from "next-auth/react" or create a server action that calls `signOut` from `@/lib/auth`.

       - Sidebar container styling:
         - width: w-[280px], flex-shrink-0
         - background: bg-[var(--sidebar-background)] or bg-[#FAF9F6]
         - border-right: border-r border-border
         - flex col, full height

    4. Create `src/components/dashboard-greeting.tsx` — Per D-06 and UI-SPEC:
       - "use client" (needs client-side time)
       - Time-of-day logic per UI-SPEC: hours 5-11 = "Good morning", 12-16 = "Good afternoon", 17+ (and 0-4) = "Good evening"
       - Heading: "Good [time], Jessica" — text-2xl (24px), font-semibold (weight 600)
       - Subtitle: "Upload a complaint and discovery request to get started." — text-base (16px), text-muted-foreground (#78736A)
       - 8px gap between heading and subtitle (space-y-2)

    5. Create `src/components/upload-zone-shell.tsx` — Per D-05 and UI-SPEC Drop Zone contract:
       - "use client" (needs drag event handlers for visual feedback)
       - Dashed border: border-2 border-dashed, border-[#E5E0D8], rounded-xl (12px radius)
       - Padding: py-12 (48px vertical)
       - Content centered (flex col items-center):
         a. lucide `Upload` icon, size 32, text-muted-foreground
         b. 16px space (mt-4)
         c. "Drop files here or click to browse" — text-base, text-muted-foreground
         d. 8px space (mt-2)
         e. "PDF files up to 20MB" — text-sm, text-muted-foreground, opacity-70

       - States (Phase 1 shell — no actual upload):
         - Default: dashed border #E5E0D8
         - Mouse hover: border color transitions to accent (#C8653A), bg changes to accent/3% opacity. Use state + onMouseEnter/onMouseLeave or CSS hover.
         - Drag-over: onDragEnter/onDragOver sets state, border becomes solid + accent color, same bg tint. onDragLeave/onDrop resets.
         - Click: opens hidden file input (accept=".pdf"). No processing — just shows the file picker and does nothing with selected files.

       - Use `useState` for hover/drag states. Transition classes for smooth border/bg changes.

    6. Create `src/app/(protected)/dashboard/page.tsx` — Dashboard page:
       - Import and render `<DashboardGreeting />`
       - Import and render `<UploadZoneShell />`
       - Top padding: pt-16 (64px per UI-SPEC)
       - 32px gap between greeting and drop zone (mt-8)
  </action>
  <verify>
    <automated>cd "C:/Users/DESKTOP/discovery-project" &amp;&amp; npx next build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/layout.tsx contains "auth" import and "SidebarNav"
    - src/app/(protected)/layout.tsx contains "redirect" (auth check)
    - src/app/(protected)/layout.tsx contains "max-w-[640px]" (centered content per UI-SPEC)
    - src/app/(protected)/dashboard/page.tsx contains "DashboardGreeting" and "UploadZoneShell"
    - src/app/(protected)/dashboard/page.tsx contains "pt-16" (64px top padding per UI-SPEC)
    - src/components/dashboard-greeting.tsx contains '"use client"'
    - src/components/dashboard-greeting.tsx contains "Good morning" and "Good afternoon" and "Good evening"
    - src/components/dashboard-greeting.tsx contains "Upload a complaint and discovery request to get started."
    - src/components/sidebar-nav.tsx contains "Discovery Drafter"
    - src/components/sidebar-nav.tsx contains "No cases yet" (D-08 empty state)
    - src/components/sidebar-nav.tsx contains "Log out" (D-13 logout text)
    - src/components/sidebar-nav.tsx contains "LogOut" (lucide icon import)
    - src/components/sidebar-nav.tsx contains "w-[280px]" (sidebar width per UI-SPEC)
    - src/components/upload-zone-shell.tsx contains "Drop files here or click to browse"
    - src/components/upload-zone-shell.tsx contains "PDF files up to 20MB"
    - src/components/upload-zone-shell.tsx contains "Upload" (lucide icon import)
    - src/components/upload-zone-shell.tsx contains "border-dashed" (dashed border per UI-SPEC)
    - src/components/upload-zone-shell.tsx contains "onDragEnter" or "onDragOver" (drag feedback)
    - src/app/page.tsx contains "redirect" and "/dashboard"
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Dashboard renders at /dashboard with sidebar (280px, "Discovery Drafter" header, "No cases yet" empty state, "Log out" button) and main area (time-of-day greeting, upload drop zone with drag-hover visual feedback). Logout ends session and redirects to /login. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 1 auth flow and UI</name>
  <files>N/A — verification only, no files modified</files>
  <action>
    Human visually verifies the complete Phase 1 auth flow and UI against the UI-SPEC design contract. This checkpoint covers both Plan 02 (login page) and Plan 03 (dashboard) as an integrated end-to-end check.
  </action>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (design contract to verify against)
  </read_first>
  <what-built>
    Complete Phase 1 Foundation: login page, authentication flow, dashboard with sidebar, greeting, drop zone shell, and logout. All built per UI-SPEC design contract with Claude-inspired warm neutral theme.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. Verify redirect to /login (unauthenticated)
    3. Login page: Cream background, centered white card, "Discovery Drafter" text, email + password fields, "Sign in" button with accent color (#C8653A)
    4. Enter wrong password — verify inline red error text "Invalid email or password." appears below password field (NO toast notification)
    5. Enter correct credentials (from .env.local) — verify redirect to dashboard
    6. Dashboard: Left sidebar (280px, "Discovery Drafter", "No cases yet", "Log out" at bottom) + main area (time-based greeting "Good [time], Jessica", subtitle, dashed-border drop zone)
    7. Hover over drop zone — verify border color changes to accent
    8. Drag a file over drop zone — verify border becomes solid accent
    9. Click drop zone — verify file picker opens (accept PDF only)
    10. Refresh browser — verify still logged in (session persists, AUTH-03)
    11. Click "Log out" — verify redirect to /login
    12. Try visiting /dashboard directly while logged out — verify redirect to /login (AUTH-02)
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: human visual verification required"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms login page matches UI-SPEC design contract
    - Human confirms dashboard layout matches UI-SPEC design contract
    - Human confirms auth flow works end-to-end (login, session persist, logout, redirect)
    - Human types "approved"
  </acceptance_criteria>
  <done>Human has verified the complete Phase 1 UI and auth flow against the UI-SPEC design contract and confirmed approval.</done>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Authenticated dashboard renders with sidebar + main content
- Time-of-day greeting displays correctly
- Sidebar shows empty state text
- Drop zone has visual drag-hover feedback
- Logout works and redirects to login
- Session persists across refresh
- `npx next build` succeeds
</verification>

<success_criteria>
- Dashboard layout matches UI-SPEC (280px sidebar, 640px centered content, 64px top padding)
- Greeting shows correct time-of-day prefix (AUTH-03 session validates user)
- Sidebar shows "No cases yet" per D-08
- Drop zone shows per D-05 with hover/drag states
- Logout per D-13 (subtle, functional)
- Human approves visual/functional quality
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
